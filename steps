git clone link
cd folder name
git status
git add filename
git commit -m"sometext"
git push origin main
git pull -----it is used to pull the data from git

cd .. ---to change the next folder
cd name of the folder that we want to swap
git init ---to initialize the folder

git branch
git branch -m main (change branch to main)
git branch -a (to check all available branches)
git checkout git-branch-1(to swap branch)
